# Домашнее задание
# Я начал проходить курс по Python в автоматизации и восьмая лекция у меня была функции,
# в данную лекцию входили такие разделы как:
# Функции в Python
# Создание и вызов функций
# Аргументы функций
# Аргументы с дефолтными значениями
# Возвращение данных из функций
# Декораторы
# Мне нужно закрепить данную тему и все разделы в ней. Задачи должны быть максимально простые,
# чтобы я закреплял синтаксис и логику, сложные задачи не давай. Выдавай мне в случайном порядке
# по одной задаче из списка разделов данной темы, без решения.
# Когда подсказываешь, не нужно приводить правильные примеры решения задачи, пока я этого не попрошу,
# до тех пор пиши какие доработки мне нужно сделать, чтобы получить правильное решение.
# Задачи выдай по запросу, когда я это напишу и нумеруй задачи.
from urllib3.util import wait_for_read


# 1. Создание и вызов функций
# Напиши функцию print_welcome(), которая выводит в консоль текст: "Добро пожаловать!". Вызови эту функцию.

def print_welcome():
    print("Добро пожаловать!")


print_welcome()

# 2. Аргументы функций
# Напиши функцию greet(name), которая принимает имя (строка) и выводит: "Привет, [name]!".
# Например, greet("Анна") → "Привет, Анна!".

def greet(name):
    print(f"Привет, {name}!")


greet("Анна")

# 3. Аргументы с дефолтными значениями
# Напиши функцию order_food(dish, count=1), где:
# dish (строка) — название блюда,
# count (число) — количество, по умолчанию 1.
# Функция выводит: "Заказано: [count] порций [dish]".
# Пример вызова без count: order_food("суши") → "Заказано: 1 порций суши".

def order_food(dish, count=1):
    print(f"Заказано: {count} порций {dish}")

order_food("Sushi", 4)

# Возвращение данных из функций
# Напиши функцию multiply(a, b), которая принимает два числа и возвращает их произведение.
# Не выводит результат, а возвращает!
# Проверку вызова (например, print(multiply(3, 4))) писать не нужно — только саму функцию.

def multiply(a, b):
    return a * b

# Декораторы
# Напиши декоратор uppercase_decorator, который преобразует результат функции к верхнему регистру.

def uppercase_decorator(func):
    def wrapper():
        result = func()
        result = result.upper()
        return result
    return wrapper