# Домашнее задание
# Я начал проходить курс по Python в автоматизации и четвертая лекция у меня была условные операторы, в данную лекцию входили такие разделы как:
#
# Логические операции
# Условные операторы
# Оператор if
# Оператор else
# Оператор elif
# Логические операции в условиях
# Тернарный оператор
# Мне нужно закрепить данную тему и все разделы в ней. Задачи должны быть максимально простые, чтобы я закреплял синтаксис и логику, сложные задачи не давай. Выдавай мне в случайном порядке по одной задаче из списка разделов данной темы, без решения.
#
# Когда подсказываешь, не нужно приводить правильные примеры решения задачи, пока я этого не попрошу, до тех пор пиши какие доработки мне нужно сделать, чтобы получить правильное решение.
#
# Задачи выдай по запросу, когда я это напишу и нумеруй задачи.
from selenium.webdriver.support.expected_conditions import element_selection_state_to_be, element_attribute_to_include

# Задача 1 (Условные операторы, оператор if)
# Напиши код, который проверяет, является ли число x положительным.
# Если это так, выведи строку "Число положительное".

x = 5
if x > 0:
    print("Число положительное")

# Задача 2
# Напиши код, который проверяет, делится ли число num на 2 без остатка.
# Если делится — выведи "Число чётное", иначе — "Число нечётное".

x = 10
if x % 2 == 0:
    print("Число чётное")

else:
    print("Число нечётное")

# Задача 3
# Напиши код, который проверяет температуру воды t
# (в градусах Цельсия) и выводит её состояние:
# Если t <= 0 — "Лёд",
# Если 0 < t < 100 — "Вода",
# Иначе — "Пар".

t = 0
if t <= 0:
    print("Лёд")
elif 0 < t < 100:
    print("Вода")
else:
    print("Пар")

# Задача 4
# Даны два логических значения (bool1 и bool2).
# Напиши код, который выводит "Оба истинны", если оба значения равны True,
# и "Хотя бы один ложный" в остальных случаях.

bool1 = True
bool2 = True

if bool1 and bool2:
    print("Оба истинны")
else:
    print("Хотя бы один ложный")

# Задача 5
# Перепиши код из задачи 4 (про bool1 и bool2) с использованием тернарного
# оператора в одну строку.

bool1 = True
bool2 = True

result = "Оба истинны" if bool1 and bool2 else "Хотя бы один ложный"
print(result)

# Задача 6 (комбинация if + логические операторы)
# Условие:
# Даны три переменные: a, b, c. Напиши код, который проверяет:
# Если a больше b и одновременно a больше c, выводит "А — максимальное";
# Иначе если b больше c, выводит "B — максимальное";
# В остальных случаях — "C — максимальное".

a = 4
b = 6
c = 3

if a > b and a > c:
    print("А — максимальное")
elif b > c:
    print("B — максимальное")
else:
    print("C — максимальное")

# Задача 7 (Работа со строками + if)
# Напиши код, который проверяет длину строки word и выводит:
# "Короткое", если длина меньше 5 символов
# "Среднее", если длина от 5 до 8 символов
# "Длинное", если длина больше 8 символов

word = "afsafa"

if len(word) < 5:
    print("Короткое")
elif 5 <= len(word) <= 8:
    print("Среднее")
else:
    print("Длинное")

# Задача 8 (Числа + вложенные условия)
# Напиши код, который проверяет число num и выводит:
# "Чётное и положительное" — если число чётное и больше нуля;
# "Нечётное и положительное" — если число нечётное и больше нуля;
# "Отрицательное" — если число меньше нуля;
# "Ноль" — если число равно нулю.

num = 7
if num < 0:
    print("Отрицательное")
elif num == 0:
    print("Ноль")
elif num % 2 == 0:  # сюда попадём только если num > 0
    print("Чётное и положительное")
else:
    print("Нечётное и положительное")

# Задача 9 (Тернарный оператор + вложенные условия)
# Перепиши решение задачи 8 (про чётность и знак числа), но:
# Используй только тернарные операторы (без if-elif-else);
# Результат сохрани в переменную result и выведи её.

result = ("Отрицательное" if num < 0 else
         "Ноль" if num == 0 else
         "Чётное и положительное" if num % 2 == 0 else
         "Нечётное и положительное")